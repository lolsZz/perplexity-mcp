/**
 * Expands the tilde (~) in a path to the user's home directory
 */
export declare function expandHomeDir(path: string): string;
/**
 * Ensures a string ends with .js extension for ESM imports
 */
export declare function ensureJsExtension(path: string): string;
